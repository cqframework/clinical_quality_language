// Most build configuration comes from the cql-all parent build!

generateSources{
    inputs.dir "${projectDir}/schema"

    doLast {
        ant.xjc(destdir: xjc.destDir, schema: "${projectDir}/schema/quick.xsd", binding: "${projectDir}/schema/quick-binding.xjb") {
            arg(line: xjc.args)
        }
    }
}
